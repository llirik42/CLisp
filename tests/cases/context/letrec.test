******************[TESTING CODE]******************
(display (letrec ((factorial
           (lambda (n)
             (if (== n 0)
                 1
                 (* n (factorial (- n 1)))))))
  (factorial 5)))

(display (letrec ((even
           (lambda (n)
             (if (== n 0)
                 #t
                 (odd (- n 1)))))
         (odd
           (lambda (n)
             (if (== n 0)
                 #f
                 (even (- n 1))))))
  (even 4)))

(display (letrec ((a (lambda (n) (if (== n 0) 1 (b (- n 1)))))
         (b (lambda (n) (if (== n 0) 0 (a (- n 1))))))
  (a 5)))

******************[EXPECTED OUT]******************
120
true
0
