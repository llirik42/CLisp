******************[TESTING CODE]******************
(define (factorial n)
  (if (= n 0)
      1
      (* n (factorial (- n 1)))))

(display (factorial 5))

(define (sum-to-n n)
  (if (= n 0)
      0
      (+ n (sum-to-n (- n 1)))))

(display (sum-to-n 10))
******************[EXPECTED OUT]******************
120
55
