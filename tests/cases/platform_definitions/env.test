******************[TESTING CODE]******************
(define a -3)
(define b 2)

(define-platform (multiply)
  `int a = cl_get_int_value(cl_get_variable_value($env, "a"));`
  `int b = cl_get_int_value(cl_get_variable_value($env, "b"));`
  `return cl_make_int(a * b);`)

(display (multiply))
******************[EXPECTED OUT]******************
-6
